import React from 'react';
import TenantsTable from './../../containers/TenantsTable/TenantsTable';

const Main = (props) => {
    return (
        <div>
            <TenantsTable/>            
        </div>
    );
};

export default Main;